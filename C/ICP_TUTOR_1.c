#include <stdio.h>

//using visual studio code because of circumstances//

int  main ()
{
    int mark1, mark2, mark3,m ark4, mark5, average;
    printf ("Enter marks for 5 subjects: ");
    scanf ("%d %d %d %d %d",&mark1, &mark2, &mark3, &mark4, mark5);
    average= (mark1+mark2+mark3+mark4+mark5)/5;
    printf ("\narea of circle is %d", average);
    return 0;
}
